# full-stack-web-dev-journey
git init
git add .
git commit -m "Combining Selectors"
git pull origin main --allow-unrelated-histories
git push -u origin main


git remote add origin https://github.com/jignesh/my-project.git
git branch -M main